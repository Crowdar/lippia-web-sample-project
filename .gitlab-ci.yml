image: docker:latest

services:
  - docker:dind

before_script:
  - docker info

build:
  stage: build
  script:
    -  docker-compose -f local-docker-compose.yml up --abort-on-container-exit --exit-code-from lippia

 
